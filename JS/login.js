const userAdmin = 'admin'
const passAdmin = 'admin'

const redirect= () =>{
    //mandar al usuario a la pagina de administrador
}

function administrador()= () => {
    if (username = userAdmin, userpass= passAdmin) {
        redirect()
    }
}